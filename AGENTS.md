## Learned User Preferences

- For editor- and agent-specific workflow guidance in this repo, assume Cursor as the development environment (not generic VS Code or other IDEs).
- After substantive feature or compliance work, the user often asks to update `CHANGELOG.md` (commonly under `[Unreleased]` unless they name a version).
- For privacy and compliance flows, ship obvious in-UI success and failure feedback; the user validates with the browser console and network responses, not only copy.
- For planning and feature/modification scoping in this repo, follow the ITIL flow: GitHub Issues are Incident Records; for bug / regression / defect issues use the `itil-problem-record` skill to reproduce, root-cause, and post a Problem Record on the issue, then use the `itil-change-record` skill (always in Plan mode) to draft the Change Record. For feature / enhancement / vendor-cost issues use the `itil-service-request` skill instead — it is read-only (no code changes), researches feasibility and cost in **dollars only** (never hours/sprints/effort) against live vendor pricing pulled this session via `firecrawl` with a date stamp, and prefers open-source examples over commercial / competitor references — then proceed to `itil-change-record` once the user approves. Always wait for user approval before any implementation. For attached implementation plans, execute the provided plan exactly (do not edit the plan file), reuse existing plan to-dos, and move them through `in_progress` while executing. When investigation reveals an issue is already resolved by prior code, do not silently skip closure: gather hard evidence (fix commit SHA, file path, shipping version vs. reporter version), verify the fix in the appropriate environment (local for dev-only bugs; preview or production for environment-specific bugs) by exercising the originally reported reproduction, present the evidence in chat and on the issue, and request explicit approval via the AskQuestion (button) interface before closing the issue with cited evidence.
- AI infrastructure baseline is Cursor plugin-first; for cross-session lesson capture keep ONLY the custom `reflect` skill (`.cursor/skills/reflect/SKILL.md`) — do not reinstall the Cursor `continual-learning` plugin (its `agents-memory-updater` subagent runs for many minutes per fire and silently rewrites `AGENTS.md` without an approval gate, and `reflect` already covers the in-the-moment lesson-capture case with explicit approval before any file write). Also avoid reintroducing gstack-driven workflows.
- For release promotion (`/raise` and similar): switch to Plan Mode when gates fail or the audit is ambiguous; when raise is allowed and the run is not `--audit-only`, execute push and open the PR without asking for extra confirmation. More generally, when a multi-step request has ambiguity (workflow ordering, merge strategy, branch pruning scope, base-branch choice, etc.), pause and ask clarifying questions — strongly prefer the AskQuestion (button) interface over freeform text for any approval, mutation set selection, or multi-option decision (including inside skills), because buttons are more deterministic than parsing freeform "yes" / "ok" / "do it"; provide recommended defaults inline so the rest of the work can run end-to-end without further interruption.
- For Google Workspace-related work (directory sync, Drive/Picker, exports), reuse the existing Google OAuth web client and Cloud Console app configuration as the single source; avoid introducing or requiring a separate OAuth client per Google feature unless a platform constraint makes it unavoidable.
- When creating documents, reports, emails, or any external-facing content (audit evidence, customer deliverables, executive packets), the output represents **Columbia Cloudworks LLC** and must carry consistent professional branding: company name in titles/headers, enterprise-grade tone, no AI-attribution phrasing or informal language; product-specific documents should also reference **EquipQR** where appropriate. For audit- or questionnaire-style evidence intended as legal support, prefer screenshot-first exhibits tied to capture **URL and UTC timestamp**, and attribute the producer as **Nicholas King** on behalf of Columbia Cloudworks LLC when that attribution is required.
- For product document generation (audit packets, compliance reports, executive summaries, customer deliverables), use the Columbia Cloudworks Word automation toolkit at `C:\Users\viral\Documents\ColumbiaCloudworks\doc-automation\` instead of Google Docs. The Word toolkit produces branded DOCX + PDF with logos, headers/footers, styled tables, and embedded screenshots. Do not use `gws docs` for product deliverable generation. For single-work-order Google Docs executive packets, keep photos in the document body; place a consolidated photo-evidence section at the very end so it can be omitted from prints; for printed work orders, repeat equipment/work-order QR codes in the page footer on every page (kept separate from the main body) and include critical identifying context in page headers so detached pages remain attributable.
- When automated PR reviewers (e.g. Copilot) leave feedback, triage each comment for validity, address valid items in code, explain deferred items with rationale, then commit, push, and post a structured summary comment on the PR; when code changes are ready to push (after a commit or before deployment validation), push to the remote proactively without waiting for explicit confirmation unless a blocking issue requires user intervention first.
- For any recurring problem, warning, or failure — security scanner alerts that survived multiple fix attempts, dev-stack startup errors, CI failures — do not classify it as a "tool quirk" or apply a downstream patch on hope; perform root-cause analysis first (what is the failing component trying to accomplish? what is the actual execution path on the failing host? why did the prior fix not take effect?) and verify any proposed fix runs on the real failing path (e.g. the same shell host, the same wrapper, the same runtime version) before claiming it is resolved. Dev-stack failures are full-stop until truly green; do not proceed with downstream work or ship "professional" handwave explanations.
- Do not create documentation directories that only hold a single leaf file (e.g. a one-off folder with one markdown file); consolidate operational runbooks and checklists into existing doc trees such as `docs/ops/` or other established `docs/` sections.

## Learned Workspace Facts

- Local dev uses `dev-start.bat`, which injects secrets from 1Password into on-disk `.env` files and starts Vite on `http://localhost:8080` (Supabase on 54321/54322); reproducing an issue locally is required before claiming a fix — probe `http://localhost:8080` first, and if `dev-start.bat` / `dev-start.bat -Force` (or `dev-stop.bat` / `dev-stop.bat -Force`) cannot bring the stack up cleanly with zero warnings or errors of any kind, defer the work until the local environment is healthy. Git worktrees and agent runs should rely on those files present in the worktree because automated agents typically cannot access 1Password directly. As of 2026-04, headless contexts (Cursor Cloud Agents, GitHub Actions) inject 1Password via `OP_SERVICE_ACCOUNT_TOKEN` (service account `op-svc-equipqr-agents`, read-only on the `EquipQR Agents` vault); the token is planted in the Cursor Cloud Agents dashboard and as a **repo-level** GitHub Actions secret (NOT per-environment) so every workflow can resolve it without an `environment:` declaration. The `.github/actions/load-1p-secrets/` composite action accepts `required: false` for best-effort loads — it emits `::warning::` and exits 0 when the token is absent rather than failing the job; downstream checks must then detect unresolved literal `op://...` strings (not just empty values) to confirm the load actually happened. Workflows that depend on a composite action MUST include that action's path (e.g. `.github/actions/load-1p-secrets/**`) in their `paths:` filter or pushes that touch only the action will not retrigger the workflow. Note: GitHub Actions evaluates `${{ ... }}` expressions inside `description:` fields and bash heredocs of `action.yml`, so do not embed literal `${{ secrets.X }}` examples in composite-action descriptions — strip the `${{ }}` and write plain prose like `secrets.X`. Vendor PATs, Datadog keys, and the GCP `agent-viewer` service-account JSON all live in that vault under naming convention `equipqr-agent-{readonly|write}-{YYYY-MM}`. Local Windows MCP config (`~/.cursor/mcp.json`) is rendered from `scripts/mcp.template.json` via `op inject`; `dev-start.ps1` runs this in step 1b. Verify wiring with `scripts/op-mcp-doctor.ps1`. **`op inject` parses TWO patterns inside any input file (including JSON string values used as narrative comments)**: Bash-style `${VAR}` (where the inner content must be a valid identifier — `.` is NOT allowed, so a literal `${...}` triggers `parsing error: invalid character in template variable: .`) AND `op://...` URIs (greedily consumed as vault/item/field references — narrative text after the `op://` will be parsed as a path and crash with too-few-slashes). Templates like `scripts/mcp.template.json` must NOT contain literal `${...}` or `op://...` substrings even inside `_comment_*` JSON strings; spell those tokens out in plain English ("dollar-curly", "secret-reference URI scheme") to keep `op inject` from choking before the renderer ever runs. Cloud Agent MCP servers must be configured in the Cursor dashboard MCP tab (not via local `mcp.json`). Public `VITE_*` build-time env vars for Vercel are pushed from 1Password via `scripts/sync-vercel-from-1password.ps1`, which reads two items in the EquipQR Agents vault — `app-env-prod-public` and `app-env-preview-public` — with one text field per variable. `op read` is case-insensitive on field labels (verified empirically), so either casing resolves; the canonical convention is lowercase (`vite_supabase_url`) to match `.github/workflows/edge-functions-smoke-test.yml` and `.github/actions/load-1p-secrets/action.yml`. The same script needs `VERCEL_PROJECT_ID` and `VERCEL_ORG_ID` exported (set internally) and a logged-in `vercel` CLI session or a `vercel-token` 1P item; running `vercel env ls` requires the project context (the script sets it via env vars rather than `vercel link`). Rotation procedure: `.cursor/skills/secrets-rotation/SKILL.md`.
- Treat `/` as the canonical marketing URL; keep `/landing` as compatibility only and normalize visitors to `/` while preserving hash and query string. For domain health, keep `equipqr.app` and `www.equipqr.app` on Vercel-managed DNS targets (not legacy `216.198.79.1`), since stale A-record routing can trigger SafeBrowse/ISP filtering and TLS failures even when deployments are healthy.
- Privacy posture work in this codebase includes CCPA/CPRA-oriented gaps: California-specific policy language, a `/privacy-request` intake path backed by the `submit-privacy-request` edge function, and user-level limits on sensitive personal information enforced in scan UX and the database layer. For production evidence collection, the privacy policy is at `/privacy-policy` (not `/privacy`), the privacy request form at `/privacy-request`, and the audit log at `/dashboard/audit-log`; confirm route definitions in code before navigating to avoid dead ends.
- Solo-developer release flow: day-to-day work on `preview` only; cut a PR to `main` when releasing (CI/CD runs Supabase migrations and near-prod validation for select users at preview.equipqr.app); pushes to `preview` update that environment; after merging `main`, Vercel refreshes preview and production is promoted manually as a final gate. `.cursor/rules/branching.mdc` (alwaysApply) is the authoritative branching policy—agents must branch off `origin/preview`, target `preview` with `gh pr create --base preview` by default, and only open `preview → main` PRs on explicit release language (`/raise`, "release", "promote"); `CONTRIBUTING.md` still references a stale `dev` branch that does not exist on the remote and should not be followed. Branch protection on `preview` requires status checks plus one review; without a second reviewer the established pattern is admin-bypass squash-merge (`gh pr merge <num> --squash --delete-branch --admin`). After a `preview → main` squash-merge lands, the established post-release cleanup is to verify content equivalence (`git diff origin/main..preview` empty) and then hard-reset `preview` to `origin/main` and force-push (`git reset --hard origin/main` + `git push --force-with-lease origin preview`) so the next dev cycle starts from the clean release point; also delete `.git/PR_*.md` scratch files left behind by `gh pr create --body-file` invocations. Commits must use the GitHub noreply address `449071+viralarchitect@users.noreply.github.com` (set in global git config) because the `viralarchitect` account has "Block command line pushes that expose my email" enabled and will reject pushes that contain the real email.
- `PROJECT_ROADMAP.md` is the in-repo planning surface alongside GitHub issues and the EquipQR organization project; viewing the org project via `gh` typically needs `read:project`, and mutating project items (status, fields) needs `project` scope.
- External uptime monitoring (Better Stack) is operational at `status.equipqr.app`, monitoring both the web frontend and the Supabase Edge Function healthcheck endpoint; alert notifications go to the sole proprietor via email on incident confirmation. After Better Stack MCP auth, run a quick read-only smoke check (uptime monitors, incidents, telemetry sources/applications, telemetry error states) per `docs/ops/better-stack-monitoring.md` (MCP smoke check section), and keep timestamped run notes under `tmp/` or similar. Better Stack MCP is also wired through the local Cursor plugin at `~/.cursor/plugins/local/better-stack` (HTTP endpoint `https://mcp.betterstack.com`). Observability gotchas: the Supabase MCP `get_logs` tool returns only the last 24 hours with no time-window parameter — for older retention reach for Better Stack telemetry or the Supabase→Datadog log drain. Google OAuth consent-screen denials are NOT logged in the customer Cloud project (they live in Google's identity infrastructure); Cloud Audit Logs only show OAuth client traffic after a successful consent + token exchange, so absence of failure logs in `equipqr-prod` does not mean no failure occurred. Google APIs `admin.googleapis.com`, `oauth2.googleapis.com`, and `cloudidentity.googleapis.com` are Google **Workspace** services, not Google Cloud project services — they do NOT appear in any Cloud project's IAM & Admin → Audit Logs (Data Access) table, and writing them into a project's `auditConfigs` is a verified no-op. To capture their audit logs in Cloud Logging, a Workspace super-admin must enable data sharing at `admin.google.com` → Account → Account settings → Data sharing with Google Cloud; the resulting logs only appear under `organizations/476784721717/logs/...`, never under `projects/equipqr-prod/...` (issue #629). As of 2026-04, Workspace data sharing is enabled and the agent's viewer SA holds org-tier read access — verification queries against `organizations/476784721717` succeed under the agent's default identity.
- **Google Cloud admin posture (org `476784721717` / `columbiacloudworks.com`).** Two service accounts act as Cursor agent identities, both org-wide as of 2026-04. **Default identity** is `equipqr-cursor-agent-viewer@equipqr-prod.iam.gserviceaccount.com` — read-only across the entire org (browser, organizationViewer, folderViewer, iam.securityReviewer, logging.privateLogViewer, logging.viewAccessor, monitoring.viewer, billing.viewer, cloudasset.viewer, serviceusage.serviceUsageViewer, orgpolicy.policyViewer, securitycenter.adminViewer) plus `billing.viewer` on all four billing accounts. Use this for every read operation, including org-tier audit log queries. **Elevation identity** is `equipqr-cursor-agent-editor-pr@equipqr-prod.iam.gserviceaccount.com` — co-admin write across the org (organizationAdmin, folderAdmin, projectCreator, projectMover, orgpolicy.policyAdmin, iam.organizationRoleAdmin, iam.serviceAccountAdmin, logging.admin, monitoring.admin, cloudasset.owner, serviceusage.serviceUsageAdmin, securitycenter.admin, billing.user) plus `billing.admin` on all four billing accounts. **Only invoke the editor SA when the user explicitly asks for a change**, and always via `--impersonate-service-account=equipqr-cursor-agent-editor-pr@equipqr-prod.iam.gserviceaccount.com` on the specific command — never `gcloud config set account`. The viewer SA holds `roles/iam.serviceAccountTokenCreator` on the editor SA (project-scoped to `equipqr-prod`) so impersonation works without keys. Neither SA holds `roles/owner` or `roles/editor` (deliberate, per Google SA best practices). Workspace super-admin rights stay with humans on admin.google.com, never on either SA. Future projects under org `476784721717` inherit the full posture automatically. Full role manifest, invocation patterns, and reapply scripts (for new orgs / projects) live in `docs/ops/cloud-admin-access.md`.
- The Supabase GitHub integration auto-deploys changes to existing edge functions on push but does not auto-create new functions or auto-run migrations on the production project; new functions and pending migrations require manual CLI or MCP-based promotion to production. A Supabase-to-Datadog log drain (US5 region) is active, streaming backend service logs (edge, postgres, auth, realtime, edge functions, storage) into Datadog for production observability. Edge-function smoke-test contract: load required secrets BEFORE running auth checks (e.g. `requireSecret`/`optionalSecret` before `requireUser`) so a 500 distinguishes "secret missing/misconfigured" from a 401 "auth failed", and the smoke test's "401 means the function is wired and healthy" assertion actually proves what it claims. The contract is locked in with Deno tests in `supabase/functions/<name>/*.test.ts` (see `public-google-maps-key`); the smoke workflow is `.github/workflows/edge-functions-smoke-test.yml`. **Phantom CRLF→LF drift in `supabase/functions/**`**: on Windows hosts, running `npm run build` (or one of its plugins/scripts) silently re-saves the edge-function `*.ts` files with LF endings even though they're committed as CRLF, so `git status` shows up to ~11 "modified" files whose content is byte-identical to HEAD. Verify with `git diff --ignore-cr-at-eol -- supabase/functions/` (returns empty) or by comparing file sizes (delta exactly equals CRLF-count). NEVER stage these into a commit — a one-way LF flip will fight the rest of the repo's CRLF convention and bounce back next build. Discard with `git checkout -- supabase/functions/` before any commit, and treat the drift as a separate (still-open) `.gitattributes` / `core.autocrlf` follow-up rather than a content change.
- Google Docs work-order export requires both Drive file access and the Google Docs API scope; organizations that connected Google Workspace before Docs export shipped may retain Drive-only grants and see export failures until they reconnect with the expanded scopes. The `gws` CLI is installed on PATH with credentials encrypted at `~/.config/gws/credentials.enc`; many Google Workspace skills live under `~/.cursor/skills/gws-*`. Google Docs `replaceAllText` matches only within a single paragraph—use unique markers or append sections via chunked `gws docs +write`. On Windows, large `--json` payloads can exceed ~32K command-line length; split batch updates. The Cursor IDE browser and `gws` authenticate as different Google identities; verify `gws`-created docs with `gws docs documents get` if the browser shows Access Denied.
- On Windows dev machines, Deno is often not on `PATH` until installed; the standard install script typically places `deno.exe` under `%USERPROFILE%\.deno\bin`—prepend that directory in the shell when running `deno test` for `supabase/functions`.
- PowerShell conventions on this Windows host:
  - **No Unix file utilities in the shell.** Never invoke `head`, `tail`, `cat`, `rg`, `sed`, `awk`, `grep` directly — PowerShell rejects them and the call wastes a turn. Use the dedicated `Read`/`Grep`/`Glob`/`StrReplace` tools instead, or PowerShell-native equivalents (`Get-Content`, `Select-String`, `Select-Object -First/-Last`).
  - **PowerShell 7.3+ promotes native-command stderr writes to terminating errors** when `$ErrorActionPreference = 'Stop'` is in effect (via `$PSNativeCommandUseErrorActionPreference`). `gcloud` (and other Google Cloud SDK wrappers) emit success lines like `Activated service account credentials for: [...]` to stderr, which will abort scripts before `$LASTEXITCODE` can be read. When wrapping such tools in `Stop`-mode scripts, save → set `$false` → restore `$PSNativeCommandUseErrorActionPreference` around the invocation and treat `$LASTEXITCODE` as the sole source of truth. `scripts/render-mcp-config.ps1` is the canonical example.
  - **`dev-start.ps1` (and `dev-start.bat`) invokes its child PowerShell scripts via `powershell.exe` (Windows PowerShell 5.1), NOT `pwsh.exe` (PowerShell 7+).** Any fix targeting a dev-start child script (`scripts/render-mcp-config.ps1`, `scripts/op-mcp-doctor.ps1`, `.cursor/hooks/sync-types.ps1`, etc.) must remain compatible with 5.1 syntax and behavior. **`dev-start.ps1` itself must stay pure ASCII** — the file is saved UTF-8 *without* BOM, and PowerShell 5.1 reads BOM-less files as Windows-1252, so a single em-dash (`U+2014`, bytes `0xE2 0x80 0x94`) gets reinterpreted such that its `0x94` trailing byte acts as a smart closing quote and silently terminates a string, producing cascading bogus parse errors hundreds of lines later (`&&` "not a valid statement separator", parens in comments triggering "expression expected", etc.). Validate with `powershell.exe -NoProfile` (5.1 explicitly), not `pwsh.exe`, and rewrite the file with `[System.Text.ASCIIEncoding]` if any non-ASCII byte is introduced. Same constraint applies to other 5.1-targeted scripts.
  - **Prefer in-process `& script.ps1` invocation over `& powershell.exe -NoProfile -ExecutionPolicy Bypass -File script.ps1`** when a child PowerShell script needs to share env vars (especially `OP_SESSION_*` from 1Password). Spawning a fresh `powershell.exe` child gives that child its own environment and forces a second `op` auth handshake (and an extra biometric/desktop-app prompt for non-service-account users). `dev-start.ps1`'s 1Password env-sync + MCP-render steps are wired this way; if you reintroduce the spawn pattern there, the duplicate-prompt regression returns. `&`-invoked scripts run in a child variable scope so they can't pollute the parent, and `exit N` from inside them only sets `$LASTEXITCODE` (does NOT terminate the parent) — bracket the call with `$ErrorActionPreference = 'Continue'` + a `finally` restore if the parent uses `Stop` mode.
  - **`gcloud` on Windows is itself a PowerShell wrapper at `gcloud.ps1` that invokes `python.exe`** — the deepest layer of the stderr-as-terminating-error issue described above.
  - **`$var:` inside double-quoted PowerShell strings is parsed as a scope qualifier** (like `$env:` or `$global:`), not as a variable followed by a colon — this throws a parser error before the script even runs. When emitting log lines like `"$var: missing"`, always use `${var}:` to delimit the variable name from the trailing colon. `scripts/sync-vercel-from-1password.ps1` previously broke on exactly this.
  - **Native-command splat traps on PS 5.1.** `'npx vercel'.Split() @args` does NOT splat the way it does in PS 7 — the whole `'npx vercel'` string is passed as the command name and the call fails. Use the call operator with explicit pieces: `& npx vercel @args` or build the full argument array and invoke `& npx @argArray`. Similarly, passing a switch with an empty-string value (e.g. `--token $env:VERCEL_TOKEN` when `VERCEL_TOKEN` is unset) makes the parser consume the next positional argument as the value, silently corrupting the call — guard with a conditional that omits the flag entirely when its value is empty (`if ($env:VERCEL_TOKEN) { $cmdArgs += @('--token', $env:VERCEL_TOKEN) }`).
  - **Literal `${{` in PowerShell strings is parsed as a variable reference** and breaks `git commit -m "...${{ secrets.X }}..."` invocations before git ever runs. When a commit message, PR body, or workflow snippet must contain `${{`, write the body to a temp file and pass it via `--file` / `--body-file`, or wrap the literal in a single-quoted here-string.
  - **Bash heredoc syntax (`<<EOF`, `$(cat <<'EOF' ... EOF)`) does not work in PowerShell** — the parser treats it as an unterminated string and the call fails before git/gh ever runs. For multi-line commit messages, PR bodies, or issue bodies on this Windows host, write the body to a temp file (e.g. under `$env:TEMP`) and pass it via `git commit -F <file>`, `gh pr create --body-file <file>`, or `gh issue create --body-file <file>`. Reserve inline `-m "..."` / `--body "..."` for short single-line strings that contain no `${{` and no newlines.
- When bumping the app version in `package-lock.json`, avoid blanket `replace_all` on generic `"version": "x.y.z"` strings; unrelated dependencies can share the same semver and their entries must stay aligned with their `resolved` URLs (fix by editing only the root package blocks or reverting mistaken dependency bumps).
- `.cursor/hooks/state/` (any hook-emitted JSON), `.cursor/plans/`, and `.cursor/hooks/__pycache__/` are gitignored as local Cursor hook and agent metadata; agents may read or update these paths in a worktree, but they are not committed as product source. `.cursor/hooks/sync-types.ps1` regenerates `src/integrations/supabase/types.ts` after migration edits and is hardened against Supabase CLI stdout pollution (connection logs and version-update banners that previously corrupted the generated file, including via `dev-start.bat -Force`): the hook captures stdout+stderr to a temp file under `$env:TEMP`, extracts content between the first `^export type` and the last `^} as const`, validates both anchors before writing, atomically writes UTF-8+BOM via `[System.IO.File]::WriteAllText`, and always exits 0 (terminating errors and prettier non-zero exits log a WARNING but never block the edit pipeline).
- Evidence and document automation: browser screenshots must land in `tmp/screenshots/` (gitignored via `tmp/*`); the browser MCP screenshot tool writes to a system temp path first—copy into `tmp/screenshots/` (see the `toolbelt` skill). The Columbia Cloudworks Word automation toolkit lives at `C:\Users\viral\Documents\ColumbiaCloudworks\doc-automation\` with branded templates and scripts; generated output defaults to the project’s `tmp\documents\` with manifest image paths relative to repo root. Word COM `InlineShapes.AddPicture()` fails on header/footer Range objects; use `SeekView` plus `Selection.InlineShapes.AddPicture()` with Word visible (minimized) and set `Font.Position` before `TypeText` for baseline alignment. Texas audit questionnaire markdown lives at `docs/compliance/texas-audit-questionnaire.md`; jurisdiction evidence runs are typically filed under `docs/audit-readiness/<jurisdiction>/` (for example `docs/audit-readiness/2026-04/texas/`).
