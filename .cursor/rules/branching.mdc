---
description: "Git branching model for EquipQR — apply when creating branches, committing, pushing, or opening PRs. main is production; feature branches PR into main."
alwaysApply: true
---
# Branching Model (authoritative)

See **`docs/ops/git-and-deploy.md`** for the full solo-developer deploy loop.

## Branches

| Branch | Role | Deploys to |
|--------|------|------------|
| **`main`** | Production source of truth | equipqr.app (after Production Release Readiness + `vercel promote`) |
| **`feat/*`, `fix/*`, etc.** | Short-lived work branches | Vercel **Preview** → commit-specific `*.vercel.app` URL |
| **`preview`** | **Vercel domain anchor only** (not integration) | **`preview.equipqr.app`** when Vercel Preview domain is bound to this branch |

There is **no `dev` branch**. Do **not** use git **`preview`** as an integration branch or PR target.

## Rules for agents

1. **Branch off `main`** for new feature, fix, refactor, chore, or experimental work.
2. **All PRs target `main` by default.** Never open PRs into git **`preview`**.
3. When GitHub defaults or old docs disagree, **this rule wins** for day-to-day work.
4. Before creating a branch:
   ```powershell
   git fetch origin main
   git switch -c feat/<short-name> origin/main
   ```
5. When opening a PR:
   ```powershell
   gh pr create --base main --head feat/<short-name> --title "<conventional-commit title>"
   ```
6. **Pre-production testing:**
   - **Primary:** commit-specific Vercel Preview URL (`equipqr-<hash>-columbia-cloudworks-llc.vercel.app` pattern) after pushing a work branch.
   - **Optional:** **`preview.equipqr.app`** — fixed hostname tied to git branch **`preview`** in Vercel; not required for every feature.
   - Supabase **ephemeral PR branches** validate `supabase/**` on PRs to `main`.
   - OAuth (Google Workspace, QuickBooks): verify on the **local stack** before merge.
7. **Hotfix:** Branch off `origin/main`, PR into `main`.
8. **Local E2E before push/PR.** No push and no PR open until `local-verify-before-preview-push.mdc` passes with zero user manual steps.
9. **Worktree tempo:**
   - **Main worktree:** merge via PR to `main` after local verify (protected branch).
   - **Linked worktrees:** full merge-ready workflow per `pr-merge-ready-workflow.mdc`.
   - **Never force-push to `main`.**

See git-powershell.mdc for Windows worktree cleanup gotchas.
